saya menggunakan windows untuk me-run `Makefile`, jadi ada sedikit perbedaan(hanya 1 poin) jika di run menggunakan **linux**
Perbedaan :
1. pada target **clean**
    di windows menggunakan command `del` sedangkan linux, menggunakan command `rm`.
